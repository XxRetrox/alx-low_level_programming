#include "main.h"

/**
 * print_square - Entry
 * @size: square
 * Return: Always 0
 */

void print_square(int size)
{
	int u;
	int a;
	int b;

	u = 35;

	if (n >= 1)
		{
		for (a = 1; a <= n; a++)
			{
			for (b = 1; b <= n; b++)
				{
				_putchar (u);
				}
			_putchar ('\n');
			}
		}
	else
		{
		_putchar ('\n');
		}
}
