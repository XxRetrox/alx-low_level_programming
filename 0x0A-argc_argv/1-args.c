#include "main.h"
#include <stdio.h>

/**
 * main - Entry
 * @argc: num of arg
 * @argv: array of arg
 * Return: a
 */

int main(int argc, char __attribute__ ((unused)) **argv)
{
	int i;
	int a;

	for (i = 0; i < argc; i++)
		{
		a = i;
		}

	printf("%d\n", a);

	return (0);
}
